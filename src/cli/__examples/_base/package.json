{
  "name": "{{name}}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "test": "mm test"
  },
  "dependencies": {
    "minimotor": "^0.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.9.0",
    "vite": "^8.1.5"
  }
}
